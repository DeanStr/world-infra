[package]
name = "delivery-core"
description = "Durable delivery worker vocabulary and queue contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true

[features]
default = []
world-identity = ["dep:world-identity-core"]
uuid = ["dep:uuid"]

[dependencies]
world-identity-core = { path = "../world-identity-core", version = "0.1.0", optional = true, default-features = false }
uuid = { workspace = true, optional = true }

[lints]
workspace = true
